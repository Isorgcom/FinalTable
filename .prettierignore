# Third-party, shipped as published. Reformatting a vendored copy would make it
# undiffable against the upstream it came from.
public/vendor/

# Build and run output, not source.
graphify-out/
test-results/
playwright-report/
coverage/

# Written by the Claude Code permission prompt, git-ignored, and not ours to
# format. Without this every approval leaves format:check failing.
.claude/settings.local.json
