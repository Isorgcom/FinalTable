node_modules/

# Saved tournaments and anything else the server writes at runtime. This is
# SAVE_DIR (see .env.example), and the container mounts it as a volume.
data/*

*.log
.env
.DS_Store
Thumbs.db
.cache/

# graphify build output (regenerate with /graphify)
graphify-out/

# Playwright output
test-results/
playwright-report/
